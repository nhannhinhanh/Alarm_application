private val DarkColorScheme = darkColorScheme(
  primary = Blue80,
  secondary = Yellow80,
  tertiary = White80
)

private val LightColorScheme = lightColorScheme(
  primary = Blue40,
  secondary = Yellow40,
  tertiary = White40

  // Bạn cũng có thể tùy chỉnh thêm nếu muốn:
  // background = White80,
  // surface = White40,
  // onPrimary = Color.White,
  // onSecondary = Color.Black,
  // onTertiary = Color.Black
)
